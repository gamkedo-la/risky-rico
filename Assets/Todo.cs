using UnityEngine;

public class Todo : MonoBehaviour
{
    // TODO: spawn waves:
    // timer for wave duration    
    // timer for rest duration    
    // dictionary mapping enemies to score milestones (add them to pool of spawnable enemies)    
    // change spawnable list to SO    
    // SO var for time between spawns (ALT: only spawn on enemy destroy?)
    // timeBetweenSpawns is the "tempo" of the game - starts slow, gets faster, slows down, then ramps up near the end of a wave
    // -- split waves into "quarters" for these fast/slow spawn rates (use dictionary to map tem)
}
